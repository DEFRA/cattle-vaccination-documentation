model {
    extend animalTestingBoundedContext {
        sfObject Test {
            #future
            title 'Test'
            description 'Test carried out on one or more animals at a CPH'
            this .futureRelationship svocBoundedContext.CPH 'Test carried out on CPH' {technology: 'Salesforce LookUp'}
            this .futureRelationship aphaBoundedContext.aphaIntegrationBridge.syncTest "Sync from Sam" { technology 'TBD' }
            this .futureRelationship aphaBoundedContext.dataPlatform 'Publish data'

        }

        sfPage TestPage {
            #future
            title 'Test Page - bTB Skin Testing'
            description 'Salesforce page for APHA staff to view TB skin test records synced from Sam (Stage 2)'
            this .futureRelationship Test 'View test record'
        }
    }

    extend animalTestingBoundedContext.bTbTestingBoundedContext.bTbSkinTestingBoundedContext{
        webFrontend tbSkinTestFrontend {
            #future
            title 'bTB Skin Test Vetinary Portal'
            description "Web interface for vets to prepare for and record skin testing"
            technology "Node.js"
            style {
                icon tech:nodejs
            }
            

            this .futureRelationship tbSkinTestBackend 'Prepare and record skin testing' {technology 'HTTPS'}
        }

        backend tbSkinTestExternalApi {
            #future
            title 'bTB Skin Testing External API'
            description 'API for externals such as VDPs'
            technology "Node.js / Hapi"

            this .futureRelationship tbSkinTestBackend 'Prepare and record skin testing' {technology 'HTTPS'}
        }

        backend tbSkinTestBackend {
            #future
            title 'bTB Skin Testing Internal API'
            description "Stateless BFF proxying requests to Salesforce"
            technology "Node.js / Hapi"

            component casesRoute {
                title "Cases Route"
                description "GET|POST /cases, GET /cases/{id} — creates and retrieves TB test cases in Salesforce"
                technology "Node.js / Hapi"
                this .futureRelationship bTbSkinTestingBoundedContext.bTbSkinTestCase "manages cases" { technology 'HTTPS' }
                this .futureRelationship animalTestingBoundedContext.Test "create measurements" { technology 'HTTPS' }
            }
        }

        sfPage bTbSkinTestCasePage {
            #future
            title 'Case Page - bTB Skin Testing'

            this .futureRelationship bTbSkinTestCase 'Manage case'
            this .futureRelationship Test 'Update/View'
        }

        sfObject bTbSkinTestCase {
            #future
            title 'Case - bTB Skin Testing'
            this .futureRelationship svocBoundedContext.CPH 'Relates to'
            this .futureRelationship livestockBoundedContext.livestockApi 'GET cattleOnHolding'
            this .futureRelationship livestockBoundedContext.cads 'Get cattle on holding'
            this .futureRelationship Test 'Record of test'

            this .futureRelationship vaccinationBoundedContext.VaccinationEvent 'Check vaccination status'
        }
    }
}
